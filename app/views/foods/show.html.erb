<p>
  <strong>Title:</strong>
  <%= @food.food_name %>
</p>

<p>
  <strong>Calorie:</strong>
  <%= @food.calorie %> kcal
</p>
<p>
  <strong>Price:</strong>
  <%= @food.price %> 円
</p>

<%= link_to 'Destroy', food_path(@food), method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-secondary" %>
<% if current_user.own?(@food) %>
  <%= link_to 'Edit', "#" %> |
<% end %>
<%= link_to 'Back', foods_path %>