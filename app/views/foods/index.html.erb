<%= link_to t('.new_food'), new_food_path, class:"btn btn-primary"%>
<%= link_to t('.groups'), groups_path, class:"btn btn-secondary" %>
<%= render 'modal' %>
<div class="flex flex-wrap">
  <%= render @foods %>
</div>

<%= paginate @foods %>